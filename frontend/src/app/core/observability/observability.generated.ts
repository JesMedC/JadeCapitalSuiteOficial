// Development-safe default. Production builds replace this file before compilation.
export const frontendObservability = Object.freeze({
  dsn: '',
  release: 'development',
  environment: 'development',
  enabled: false,
});
