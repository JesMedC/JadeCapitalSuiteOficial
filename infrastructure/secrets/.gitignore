# Wave 10 slice 10.2 — production secrets live in this directory.
# NEVER commit the contents. The placeholder files exist only on the
# production VM; the dev / CI environment should never create them.
*
!.gitignore
!.gitkeep
